{% extends 'base.html.twig' %}

{% block body %}
    <h1>Friend</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ friend.id }}</td>
            </tr>
            <tr>
                <th>Id1</th>
                <td>{{ friend.id1 }}</td>
            </tr>
            <tr>
                <th>Id2</th>
                <td>{{ friend.id2 }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('friends_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('friends_edit', { 'id': friend.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
